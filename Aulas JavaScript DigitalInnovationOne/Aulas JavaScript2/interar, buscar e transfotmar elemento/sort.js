//Sort
//ordena elemetos de um array
//de acordo coma a condição

const arr = ['c', 'b', 'd', 'a'];

const arr2 = arr.sort()
    /*const arr3 = arr.sort((curret.next) = curret.arr - next.arr)*/

console.log(arr2);